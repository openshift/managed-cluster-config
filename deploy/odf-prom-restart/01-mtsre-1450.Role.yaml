---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: prometheus-restarter-role
  namespace: openshift-storage 
rules:
- apiGroups: [""]
  resources:
    - "pods"
  verbs: ["delete"]
