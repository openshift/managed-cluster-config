This subfolder is for MC alerts we only want on production, see config.yaml.

We don't want the debug handler alerts to fire on staging/integration, as we expect to have to debug handlers enabled there.