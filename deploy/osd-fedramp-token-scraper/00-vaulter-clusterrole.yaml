apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: openshift-sre-token-vaulter
rules:
  - apiGroups:
      - "hive.openshift.io"
    resources:
      - "clusterdeployments"
    verbs:
      - get
      - list
