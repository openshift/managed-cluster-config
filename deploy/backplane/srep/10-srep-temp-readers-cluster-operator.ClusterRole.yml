apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: backplane-srep-temp-readers-cluster-operator
  labels:
    managed.openshift.io/aggregate-to-backplane-srep: "true"
rules:
# SRE can get, list, and watch 'operators.operators.coreos.com'
# TODO: Temporary permission,
# Remove after https://github.com/operator-framework/operator-lifecycle-manager/pull/2175
- apiGroups:
  - operators.coreos.com
  resources:
  - operators
  verbs:
  - get
  - list
  - watch

