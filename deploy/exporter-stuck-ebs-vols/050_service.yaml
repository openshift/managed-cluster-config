apiVersion: v1
kind: Service
metadata:
  labels:
    name: sre-stuck-ebs-vols
  name: sre-stuck-ebs-vols
  namespace: openshift-monitoring
spec:
  ports:
  - name: http-main
    port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    name: sre-stuck-ebs-vols
  sessionAffinity: None
  type: ClusterIP
