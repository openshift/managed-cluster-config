apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: console-webhook-cleanup
rules:
- apiGroups:
  - "apps"
  resources:
  - deployments
  verbs:
  - get
  - delete
  resourceNames:
  - console-conversion-webhook
