# UWM-enabled management clusters
deployDir: cluster-monitoring-config/management-clusters
category: uwm

selector:
  deploymentMode: "SelectorSyncSet"
  selectorSyncSet:
    matchExpressions:
      - key: hive.openshift.io/version-major-minor
        operator: NotIn
        values: ["4.5"]
      - key: ext-managed.openshift.io/uwm-disabled
        operator: NotIn
        values: ["true"]
      - key: api.openshift.com/fedramp
        operator: NotIn
        values: ["true"]
      - key: ext-hypershift.openshift.io/cluster-type
        operator: In
        values: ["management-cluster"]

configOverrides:
  enableUserWorkload: true
  prometheusK8s:
    retention: 7d
removeKeys:
  - k8sPrometheusAdapter
