groups:
- name: sre-dns-alerts
  rules:
  - alert: DNSLatencyOver200ms5Min
    expr: max_over_time(dns_latency_milliseconds_sum[5m]) > 0.2
    labels:
      severity: critical

  - alert: DNSErrors05Min
    expr: rate(dns_failure_failure_total[5m]) > 0
    labels:
      severity: critical